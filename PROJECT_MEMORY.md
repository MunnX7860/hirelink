# Project Memory

> Condensed, always-true context for AI coding sessions. Read this first; trust it over assumptions. Expanded specs live in `docs/00`–`docs/15` — cite section numbers when a rule matters. Update this file whenever architecture, status, or conventions change (14 §7).

## What we're building
**HireLink** (working title) — the simplest mobile-first hiring & talent CRM for businesses hiring through social media. Core loop: owner creates job → gets shareable link `/apply/{slug}` → applicants apply on mobile → resumes go to the **owner's own Google Drive** → owner gets **Telegram** ping → applicant gets **email**. Phases: 0 Foundation → 1 Personal Hiring Tool (MVP) → 2 Talent CRM → 3 BYOK AI → 4 SaaS multi-tenant.

## Current state
- **Phase 0 — Foundation COMPLETE (2026-08-07).** Scaffold + auth shell + migrations 0001–0004 + CI.
- **Phase 1 — Personal Hiring Tool CODE COMPLETE (2026-08-08).** Full hiring loop; all offline gates green.
- **Phase 3 — AI (BYOK) CODE COMPLETE (2026-08-08).** `lib/ai` seam + four AI features, BYOK Settings card, full degradation matrix. Gates: 109 unit + 29 always-on e2e.
- **Phase 4 — SaaS CODE COMPLETE (2026-08-08) → v1.0.0.** Multi-tenant orgs live end-to-end (docs/11): workspace resolution cookie `hl_org` → `default_organization_id` → personal via `requireWorkspace()` (route-guard pattern that superseded `requireUser()`); orgs CRUD + soft-delete/restore + transfer; invites (sha256-hashed 7d tokens, `/invite/[token]` page, atomic `accept_org_invite` RPC w/ email-match + plan-mirrored seat cap); `lib/authz.ts` matrix (owner/admin/member; org-owner-only: branding/transfer/delete); `lib/plans.ts` (free 3/1/500 · pro 25/3/10k · team 100/25/50k) enforced as 402 `PLAN_LIMIT`; org-first integrations (`getScopedIntegration` → `{row, level}`) incl. Telegram shared bot (`TELEGRAM_SHARED_BOT_TOKEN`) + org Drive via `{org_id}` OAuth state; apply-page branding (custom for pro/team, "via HireLink" pill otherwise); job moves w/ exclusively-attached applicant re-scoping; org-first apply dedupe; `/api/cron/org-purge` 7-day re-home (04:00 UTC, second vercel.json cron); **migration 0006** (additive, re-runnable, zero data movement + guarded shell-org backfill; `scripts/backfill-orgs.mjs` standalone runner). Gates: **184 unit + 48 always-on e2e (19-route org 401 matrix) all green; X1–X10 cross-tenant suite DB-gated (self-skips without live Supabase, like seed-perf)**; build green, `/apply/[slug]` 3.56 kB. Remaining env-dependent: 0006 staging rehearsal + X-suite first green run (docs/12 checklist) — needs hosted Supabase.
- **Phase 5 — Smart Screening (docs/17) · SHIPPED as v1.2.0 · 5.0–5.6 CODE COMPLETE (2026-08-09).** 5.6 QA+scale gate: `scripts/seed-screening.mjs` (500-candidate probe, deterministic order-independent verdict distribution: i%10=0→DNMC, i%10=1→review via empty-mandatory, else qualified; synthetic parse-v2 profiles, CTC null; local/staging-only guard + --clean); `e2e/screening.spec.ts` **S1–S10** (serial, DB-gated: S1 round-trip via recompute, S2 DNMC visible, S3 review-not-rejected, S4 live small-pool reasons/evidence, S5 upper-bound never-padded, S6 flipped-failure retry leaves ok-rows identical, S7 injection neutralised, S8 append-only history, S9 409 key-free via active integration row, S10a exact 500 deterministic distribution, S10b Batch upgrade + ≤5% failures + retry; AI gate = `E2E_WITH_AI`+`E2E_AI_API_KEY`+`ENCRYPTION_SECRET` via new `e2e/helpers/ai.ts`); docs/17 **§17 Troubleshooting** added; **v1.2.0** (package.json + CHANGELOG [1.2.0] + README). Offline gates: **348 unit + 61 always-on e2e + 38 self-skipping live journeys**. ⚠️ "scale-ready" is NOT claimed until S10 passes on staging (docs/12 checkbox owns it). Earlier stages — 5.5 Drive summary artifact (17 §13): 5.5 Drive summary artifact (17 §13): **migration 0009** (`summary_folder_id`/`summary_file_id` on sessions); `StorageProvider.ensureFolder(name,parentId)` (+03 §4/07 §4·§5 doc'd); `features/screening/summary-artifact.ts` — pure `buildScreeningSummary` (dashboard-identical `groupResultsForDisplay` semantics, INSUFFICIENT_EVIDENCE verbatim, failed-rows-last) + D4-absorbed `writeScreeningSummaryArtifact` hooked at BOTH engines' completion points (write-once: retry never rewrites; no-Drive → skipped silently; integrationBroken → banner); session read models + dashboard "Summary in Drive ↗" link. Gates: **348 unit + 61 always-on e2e all green**; screening page 8.16 kB. Next: 5.6 seed script + S1–S10 incl. 500-candidate live scale gate → v1.2.0. 5.4 async large pools: **migration 0008** (`locked_at` lease + `screening_sessions_active_idx` partial); **CAS processing lease** (`isLeaseClaimable` — 10-min crash reclaim, 2-min quota cooldown, clean-slice release); `/api/cron/screening-worker` (org-purge pattern; scans ≤10, claims ≤3, ≤50 s slices inside a 45 s budget, service client allowed-zone) + `vercel.json` per-minute cron; **advance-on-view** on `GET /api/ai/screenings/:id` (dashboard 3 s polling = Hobby self-healer); **Gemini Batch accelerator** (`lib/ai/batch.ts` plain-REST, key-in-header, BOTH documented poll shapes normalized, 15 MB inline guard, 400-class→interactive fallback; one prompt/candidate, `metadata.key`=row id, `parseBatchItemResult` safe-rail drain; pending ≥ 50 real-volume gate); **retry/cancel endpoints** (retry flips only failed rows + resets engine provenance, 202/200-noop/409; cancel stops in-flight + best-effort remote batch cancel, 409 terminal); dashboard Retry/Stop (two-tap) + Batch badge. Gates: **342 unit + 61 always-on e2e all green**; screening page 8.02 kB; `/apply/[slug]` 5.63 kB unchanged. Earlier stages — 5.1 questionnaire + deterministic engine: `jobs.screening_config` (PRIVATE rules; `sanitizeQuestions()` is the ONLY candidate-facing projection — Q7), pure `features/screening/engine.ts` (safety rail: any clear fail → `does_not_meet_mandatory`; any ambiguous/empty → `review_required`; no mandatory → NULL; 100 % branch-covered), verdicts on `applications.screening_status` (pipeline status stays human-owned), apply multipart `answers` field → 400 `VALIDATION_ERROR` w/ `answers.<id>` errors, first-write-wins, D4-absorbed persistence; builder + sync re-run (200 `{recomputed,changed}`, NOT the §9 worker) + answers card; routes screening-config/-recompute/answers; **migration 0007** (answers/profiles/sessions/results + RLS). 5.2 resume profiles: `profile_extract` capability + `profile_extract.v1` prompt (CTC-only-when-explicit, no-implied-skills), `ResumeProfileSchema` heal-on-rails, `applicant_profiles` cache w/ `profileStale` (newer resume or prompt drift ONLY; fresh = no model call, no key needed), `POST /api/ai/applicant-profile`, collapsible card on applicant page (hidden w/o active key). 5.3 AI screening sessions: `POST/GET /api/ai/screenings(+/:id)` (409 per-job active guard, 400 AI_NOT_CONFIGURED), pool snapshot to pending rows, chunked engine (one call per ≤8 anonymized C1…C8 candidates; profile+answers+resume-excerpt packing; ~4 s pace; 429 → backoff → `quota_limited`; key-broken → session failed; row failures never sink the session), `after()` kickoff at create (maxDuration 60; worker = 5.4), `screen_candidates.v1` + `ScreeningResultSchema` safe-rail heals, top-N upper-bound in the read model, new `/dashboard/jobs/[id]/screening` page (counters, sessions list, live progress, expandable reasons/evidence/gaps) + Screening card on job detail. (5.1–5.3 gates then: 316 unit + 58 e2e.)
- **Gap-closure pass · v1.3.0 (2026-08-10).** Full audit against docs/00–17 (18 review agents) surfaced and this pass fixed: critical `sendEmail()` From-header bug (malformed RFC 5322 address whenever a brand name was set — `buildFromHeader()` now); Telegram 401/403 never flipped `integration.status='error'` (now does, via `markIntegrationError` in `Notifications.telegram()`); `alertOwnerResumeFailed` skipped the retry+journal policy every other notification follows (now matches `sendApplicantConfirmation`'s pattern); Drive OAuth refresh tokens were never persisted (misleading comment claimed a nonexistent "Phase 2" deferral — `driveOAuthClientFor()` in `lib/integrations/resolve.ts` now attaches a `tokens` listener + re-encrypts on refresh, used by all 3 call sites); no timeout on Drive API calls (30s added, matches Telegram 8s/Gemini 30s elsewhere). **Two docs/02+docs/11-specified features that were never built, now are**: account deletion + data export (`DELETE`/`GET /api/users/me[/export]`, `features/settings/server.ts` — blocks with 409 if the caller still owns a live org or org-scoped jobs/applicants/tags/integrations, so one member's account deletion can never cascade-delete a team's shared data) and the Phase 4 one-time workspace chooser (**migration 0010**: `users.workspace_onboarded_at` is the "has explicitly chosen" signal `default_organization_id IS NULL` alone couldn't provide; `needsWorkspaceChooser()` + `WorkspaceChooserModal` mounted in the app shell). **Progressive enhancement** (docs/06 §6): the public apply form now has a real `action`/`method` fallback and works with JS disabled — `POST /api/apply/:slug` tells a plain-form navigation apart from the enhanced XHR path via the standard `Sec-Fetch-Mode: navigate` header (zero client cooperation needed, doesn't touch any existing JSON-consuming caller); drag-and-drop resume upload at `>=md`; org `primary_color` now reaches the apply button, not just the header. **CI/deployment infra that existed only in docs, now real**: `migrate.yml` (staging→prod, pg_dump snapshots, secrets-gated no-op until configured), `weekly-integrations.yml` + `e2e/weekly-integrations.spec.ts` (real Drive/Telegram/Resend contract checks, per-provider self-skip), `ci.yml` gains a `migrations` job (every PR applies `supabase/migrations/*` to a throwaway local Postgres — this literally never happened in CI before), Lighthouse CI warn-only on a new static `/apply/demo` fixture (`lighthouserc.json`), `@vitest/coverage-v8` installed (`pnpm test:coverage` — reveals `src/lib/**` at 77–100% but `src/features/**/server.ts` at ~0–8%, since those are e2e-covered by design per docs/13, not a hidden regression). Plus: shared `src/ui/select.tsx` adopted in 4 call sites, `loading.tsx` skeletons on 6 routes, FAB added to Inbox, "Candidate view" preview toggle on the questionnaire builder, `/privacy` page, cron `timingSafeEqual`, ESLint service-client zone narrowed to the callback route + the new justified `users/me` deletion route, missing env vars added to `.env.example`, and a cluster of stale doc cross-references fixed (docs/04 ERD + migration table, docs/12 env matrix + local-dev quickstart, `supabase/seed.sql`'s dangling reference). **Gates: 356 unit (was 348) + typecheck/lint/format/build all green**; e2e count unchanged — no-JS fallback and workspace chooser are implemented + build-verified but don't have new Playwright coverage yet (need a `javaScriptEnabled:false` context and a live-Supabase second-org fixture respectively). Full detail: CHANGELOG [1.3.0].
- **RLS recursion hotfix · v1.3.0 unreleased (2026-08-12).** First-ever live deploy against a real Supabase project surfaced `42P17: infinite recursion detected in policy` on every query touching `organizations`/`organization_members` (anon-key `/api/health` reported `db:false`) — the DB-gated X-suite that would normally catch this self-skips without live Supabase, so migrations 0004's original policies had never actually been exercised end-to-end before. Root cause: `organizations_member_read` (on `organizations`) and `organization_members_org_owner` (on `organization_members`) queried each other's table directly in their `USING` clauses with no security-definer bypass — a genuine 2-table RLS cycle. **Migration 0011** routes both through `security definer` `plpgsql` helpers (`current_org_role`, new `is_org_owner`); `plpgsql` specifically, not `language sql`, because single-statement SQL-language functions are eligible for planner inlining that can silently collapse the security-definer boundary. Verified live post-`db push`: `/api/health` → `db:true`. Detail: CHANGELOG [Unreleased].
- **Git initialized 2026-08-09** (`main` = v1.0.0 baseline `6da917c`; work on branch `feature/ai-candidate-screening` — 5.1 commit `a0a81d0`; commit identity `agent@hirelink.local` / `Arena Agent`; sandbox artifacts `.npm/` untracked + ignored).
- Workspace: `docs/` = normative specs; `uploads/` = original skeletons (do not edit); root = app code.
- Key entry points: apply orchestration `src/features/applications/server.ts`; jobs `src/features/jobs/server.ts`; providers `src/lib/{storage,notifications,integrations}`; public surface `src/app/(public)/apply/[slug]` + `src/app/api/apply/[slug]`.
- Sandbox quirks (IMPORTANT, sessions reset): 1.9 GB RAM — run `sudo fallocate -l 3G /swapfile && sudo chmod 600 /swapfile && sudo mkswap /swapfile && sudo swapon /swapfile` before builds (dropped between sessions); `.cache/` + `/usr` WIPED between sessions → reinstall `npx playwright install chromium` + `sudo npx playwright install-deps chromium` for e2e; typecheck needs `NODE_OPTIONS='--max-old-space-size=1280'`; no pnpm binary (npm used; CI uses pnpm); `.env.local` = documented dummies (health reports db:false without Supabase).
- Build knobs in `next.config.ts`: serverExternalPackages(googleapis/google-auth-library/resend/react-email/upstash) + webpackBuildWorker:false + build skips lint/tsc (CI runs them) + webpack cache off. googleapis via SUBPATH imports only (`googleapis/build/src/apis/drive`, `google-auth-library`) — full-index imports OOM typecheck.

## Locked decisions (D1–D8, details in docs/00 §9)
1. Login = Supabase Auth (Google); Drive = **separate** incremental OAuth, scope `drive.file` only, offline refresh token.
2. Files live in user-owned Google Drive; DB stores `storage_file_id`; **no public file URLs ever** — resumes stream via authed route.
3. User credentials (Drive/Telegram/AI keys) = AES-256-GCM via `ENCRYPTION_SECRET`, format `v1.iv.tag.ct`, in `integrations.credentials_encrypted`. Write-only APIs.
4. Notifications/Drive/AI failures **never block** submissions — degrade, write `timeline_events`, retry once, surface banner.
5. Multi-tenancy active since v1.0.0: every owned table has `owner_id` + nullable `organization_id`; RLS everywhere (`current_org_role()` security-definer gates additive org policies); scopes personal-vs-org partition every read/write (personal = org IS NULL).
6. Application statuses: `new, reviewing, shortlisted, interview, offered, hired, rejected, archived`. Job statuses: `draft, active, closed`.
7. AI = BYOK only (user's Gemini key, `gemini-2.0-flash` default); every feature works with AI disabled.
8. Apply endpoint: public, rate-limited (5/10min/IP via Upstash, env-gated), honeypot, idempotent on `(job_id, applicant_id)`; duplicates return success with `already_applied`.

## Stack (locked, docs/03 §2)
Next.js 15 App Router · React 19 · TS strict · Tailwind 4 · Supabase Postgres 15 (RLS) · Vercel · Resend (react-email) · Telegram Bot API · zod (shared schemas) · TanStack Query · react-hook-form · Vitest + Playwright · Node 20, pnpm.

## Non-negotiable invariants
- RLS on every table; service-role client only in: apply route, integration callbacks (OAuth redirect endpoints only, not the whole `integrations/**` tree), cron, signed webhooks, and self account deletion (`DELETE /api/users/me` — needs the GoTrue Admin API; every query still filters by the caller's own verified id).
- `process.env` read in `lib/env.ts` only; route handlers are thin (validate → service → shape); no SQL/SDK in `app/`.
- Workspace-scoped endpoints: unknown-or-out-of-scope ID = 404 (no existence leaks); errors = `{ error: { code, message, details?, request_id } }`; 402 `PLAN_LIMIT` for plan caps (details `{limit_key, used, limit, plan}`); 409 `ALREADY_MEMBER`; 410 `INVITE_EXPIRED`.
- Phase 4 architecture: scope filtering in services via `features/orgs/scope.ts` (`applyScope`, `jobEmbedFilters` (jobs!inner), `filterApplicationIdsInScope`, `refForScope`); timeline feed is a dual-branch JS merge (applicant!inner ∪ application→job!inner) with cursor on both; timeline events are written with `owner_id` = acting user (never data-owner) so RLS insert policies hold for members.
- Resume uploads: pdf/doc/docx by magic bytes, ≤ 10 MB; idempotent per application.
- Screening (Phase 5): verdicts ONLY on `applications.screening_status`; rules/classification NEVER leave the server (sanitize before public projection); engine can never silently reject — ambiguity is always `review_required`; candidates are never deleted for failing.
- Mobile-first (360px), one primary action per screen, tokens from docs/06 §2.
- Conventional Commits; CHANGELOG every user-visible change; update THIS file on architecture shifts.

## Key facts cheat-sheet
- Env vars: `NEXT_PUBLIC_APP_URL`, `NEXT_PUBLIC_SUPABASE_URL/ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `GOOGLE_CLIENT_ID/SECRET` (optional at boot, but Drive is the core storage loop — see docs/12 §2 Notes), `ENCRYPTION_SECRET` (64-hex), `RESEND_API_KEY`, `EMAIL_FROM`, `UPSTASH_*` (opt), `SENTRY_DSN` (opt), `CRON_SECRET` + `RESEND_WEBHOOK_SECRET` (Phase 2), `TELEGRAM_SHARED_BOT_TOKEN` (Phase 4, opt). All present in `.env.example`.
- Drive layout: `{root}/Jobs/{Title}—{jobId[:8]}/{Name}—{applicantId[:8]}—file.pdf`; root = `integrations.config.root_folder_id`.
- Telegram: per-owner BotFather bot (token encrypted, `config.chat_id`); message template + inline button in docs/08 §2.
- Email catalogue: `application_received` (Phase 1), `owner_resume_failed`; `interview_invitation`/`rejection` = future; registry pattern docs/09 §2.
- Migrations: `0001` extensions/enums → `0002` tables (org tables pre-created, "activated Phase 4") → `0003` indexes → `0004` RLS+trigger → `0005` app indexes+enum → `0006` Phase 4 org activation (invites, soft-delete, org indexes, helpers, additive org policies, shell-org backfill) → `0007` Phase 5 screening (questionnaire config + verdict column + answers/profiles/sessions/results + RLS) → `0008` screening async (processing lease) → `0009` screening Drive summary artifact (folder/file id cache) → `0010` workspace chooser (`users.workspace_onboarded_at` + `accept_org_invite` re-creation) → `0011` fix org/org_members RLS recursion (`is_org_owner()` helper + policy rewrite, `current_org_role` moved to plpgsql).
- Perf budgets: apply page LCP ≤ 2.0s / ≤ 60KB JS; dashboard ≤ 2.5s / ≤ 120KB.
- Working title only — rename before public launch.
